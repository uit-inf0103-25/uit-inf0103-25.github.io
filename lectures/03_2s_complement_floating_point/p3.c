#include <stdio.h>

int main()
{
    double a = 0.1;
    double b = 0.2;
    double c = 0.3;
    double sum = a + b;

    printf("sum = %.20g\n", sum);
    printf("c   = %.20g\n", c);

    return 0;
}