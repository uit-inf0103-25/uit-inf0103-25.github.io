Lag en funksjon som tar inn ett fødselsnummer (11 siffer) - gjerne ved hjelp av flere argumenter, og lager en struct som inneholder fødselsdag, måned, år, kjønn, og alder.

e.g.:

```c
typedef struct {
    // Fyll inn her
} person_t;

person_t legg_til_person(int dag, int maaned, int aar, int individnr, int kontroll) {
    // Fyll inn her
}
```

Bonus: Legg til et felt som forteller om fødselsnummeret er gyldig eller ikke.
